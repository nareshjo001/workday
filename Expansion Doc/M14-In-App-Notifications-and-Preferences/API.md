# API

Each role namespace exposes `GET /notifications`, `PATCH /notifications/:id/read`, `PATCH /notifications/read-all`, `GET /notification-preferences`, and `PUT /notification-preferences/:eventType`. All use the JWT identity only.
