# Testing

The M14 integration test verifies scoped candidate notification delivery, inbox ownership, read transitions, and preference updates. Full backend coverage and frontend lint/build remain required regression gates.
